# ---- Core API ----
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
python-multipart>=0.0.9        # required for File/UploadFile (multipart/form-data)
pydantic>=2.6.0
pydantic-settings>=2.2.0       # for .env / config management

# ---- Database & Spatial (PostgreSQL + PostGIS) ----
sqlalchemy>=2.0.28
geoalchemy2>=0.14.6            # PostGIS ORM support on top of SQLAlchemy
psycopg2-binary>=2.9.9         # sync driver (use asyncpg instead if going fully async)
alembic>=1.13.1                # DB migrations

# ---- Auth / Security ----
python-jose[cryptography]>=3.3.0   # JWT tokens for role-based logins
passlib[bcrypt]>=1.7.4             # password hashing

# ---- Image Handling & Preprocessing ----
pillow>=10.0.0
exifread>=3.0.0                # robust EXIF/GPS metadata extraction from photos
opencv-python-headless>=4.9.0  # image ops for severity/segmentation pipeline

# ---- AI / ML (Pothole & Flooding Detection) ----
torch>=2.2.0                   # CPU build by default; swap for CUDA wheel if GPU available
torchvision>=0.17.0
ultralytics>=8.1.0              # YOLOv8 for pothole/debris/waterlogging detection
numpy>=1.26.0

# ---- NLP (text/voice description keyword extraction) ----
transformers>=4.38.0            # optional: for multilingual keyword/urgency extraction
langdetect>=1.0.9               # quick language detection (Hindi/regional/English)

# ---- Geocoding / Maps ----
geopy>=2.4.1                    # reverse geocoding helper (wraps Nominatim/Google APIs)
shapely>=2.0.3                  # geometry ops alongside PostGIS

# ---- Notifications ----
firebase-admin>=6.5.0           # push notifications (FCM) for status updates

# ---- Weather / Predictive Layer ----
requests>=2.31.0                # calling IMD/weather APIs for flood prediction

# ---- Dev & Testing ----
pytest>=8.0.0
httpx>=0.27.0                   # for FastAPI TestClient / async requests
python-dotenv>=1.0.1
